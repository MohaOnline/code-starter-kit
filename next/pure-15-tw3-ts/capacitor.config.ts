import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'cn.net.tanan.pure15tw3ts',
  appName: 'pure-15-tw3-ts',
  webDir: 'public'
};

export default config;

// npx cap add android
// npx cap add ios