# 用户需求

配置 Bun 来运行 Next.js 15 的应用，是运行，不是光启动。

## 理解

- 使用 Bun 作为运行时来执行 Next.js 15 应用。
- 强调实际运行应用，而不是仅启动服务器。
- 项目基于 Next.js 15 和 App Router。

## 计划

- 检查当前 package.json 的 scripts。
- 配置 scripts 以支持 Bun 运行 dev, build, start 等。
- 生成文档说明如何使用 Bun 运行应用。